# SubScript 订阅管家

## 本地运行

前置条件：Node.js

1. 安装依赖

   ```bash
   npm install
   ```

2. 可选：复制 `.env.example` 为 `.env.local`，并配置 `VITE_WORKER_URL` 为你的 Cloudflare Worker 地址。

3. 启动开发环境

   ```bash
   npm run dev
   ```

AI 凭据在应用设置中配置，不要提交真实密钥。
